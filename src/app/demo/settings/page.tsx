import { SettingsContent } from "../_components/settings-content";

export const metadata = {
  title: "Settings · Blox Pay Demo",
};

export default function SettingsPage() {
  return <SettingsContent />;
}
